{
  "name": "genie-dashboard",
  "version": "3.1.0",
  "description": "Genie Task Dashboard - Kanban for AI tasks with real-time sync",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:local": "LOCAL_MODE=true node server.js",
    "backup": "curl -X POST http://localhost:3456/api/backup",
    "migrate": "node supabase/migrate.js",
    "deploy:staging": "./deploy.sh staging",
    "deploy:production": "./deploy.sh production"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "dotenv": "^17.2.3"
  }
}
